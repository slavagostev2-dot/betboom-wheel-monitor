# BetBoom Wheel Monitor

Монитор публичных Telegram-источников, работающий в GitHub Actions без VPS и без постоянно включённого компьютера.

## Главное

- старые адреса `betboom.ru/freestream/...` отдельно не проверяются;
- поиск начинается только с новых сообщений Telegram;
- одинаковое колесо из разных каналов присылается один раз;
- основной процесс проверяет Telegram примерно каждые 5 минут;
- бот периодически подтверждает, что автоматический процесс работает;
- ночной поиск проверяет отдельный список кандидатов и сразу уведомляет о найденном колесе.

## Файлы

```text
.github/workflows/monitor.yml            непрерывный быстрый монитор
.github/workflows/nightly-discovery.yml  ночная проверка кандидатов
monitor.py                               основной код
nightly_discovery.py                     ночное обнаружение и перенос источников
telegram_monitor.py                      совместимый запуск monitor.py
public_sources.txt                       быстрые источники
source_catalog.txt                       отдельные ночные кандидаты
identifier_sources.json                  подписи и сопоставления, не список старых ссылок
state.json                               рабочее состояние, создаётся/обновляется workflow
discovery_state.json                     состояние ночного поиска, создаётся workflow
self_test.py                             автономные проверки проекта
requirements.txt                         зависимости
PLAN_RU.md                               план следующих этапов
```

## Автоматический режим

После загрузки изменённых файлов в ветку `main` workflow запускается через `push`. Затем один GitHub runner выполняет несколько проверок с интервалом около пяти минут. Перед шестичасовым ограничением он вызывает следующую смену через `workflow_dispatch`.

Резервный cron запускается раз в четыре часа. Ночной поиск запускается примерно в 03:27 по времени Барнаула.

При первом автоматическом запуске бот должен прислать сообщение **«Автоматический монитор работает»**. Затем такой статус приходит раз в 12 часов. Это позволяет отличить отсутствие новых колёс от остановившегося workflow.

## Дедупликация

Ключом является идентификатор после `/freestream/`. Поэтому следующие варианты считаются одним колесом:

```text
https://betboom.ru/freestream/Shoke
https://www.betboom.ru/freestream/shoke?from=telegram
```

При найденном времени повтор блокируется до прокрутки плюс 30 минут. Если время определить невозможно, блокировка действует 24 часа.

## Ночной список

`source_catalog.txt` не должен пересекаться с `public_sources.txt`. Если ночной поиск находит свежую ссылку:

1. уведомление отправляется сразу;
2. канал добавляется в `public_sources.txt`;
3. канал удаляется из `source_catalog.txt`;
4. GitHub Actions сохраняет изменения коммитом от `github-actions[bot]`.

Даже при ошибке сохранения уведомление уже отправлено, поэтому колесо не теряется.

## Установка

В репозитории должны быть Secrets:

```text
BOT_TOKEN
BOT_CHAT_ID
```

В `Settings → Actions → General → Workflow permissions` должно быть включено **Read and write permissions**.

После загрузки файлов открой `Actions → Monitor BetBoom wheels`. Первый запуск должен появиться автоматически как событие `push`. Для разовой ручной проверки можно выбрать `Run workflow`, оставив переключатель непрерывного режима выключенным.

## Ограничения

GitHub-версия читает только то, что доступно через публичный веб-просмотр `t.me/s/<username>`. Закрытые чаты, публикации без публичного preview и отдельные JavaScript-таймеры могут быть недоступны.
