function badge(x){return x.kind==='active'?'<span class="badge active">Действует</span>':'<span class="badge pending">Проверяется</span>'}
function wheelCard(x,small=false){const id=wheelKey(x),mine=joined(x),u=safeUrl(x.url),post=safeUrl(x.message_url),d=x.deadlineObj?.toISOString()||'',reason=x.reason||'';return`<article class="card wheel-card ${mine?'joined':''}"><div class="wheel-title-row"><div><div class="eyebrow">Колесо</div><h3>${esc(x.identifier||x.key)}</h3></div>${mine?'<span class="badge mine">Моё</span>':badge(x)}</div><div class="wheel-time" ${d?`data-deadline="${esc(d)}"`:''}>${esc(x.deadlineObj?left(x.deadlineObj):(x.kind==='pending'?'идёт перепроверка':'время не определено'))}</div><div class="wheel-meta"><div class="meta-cell"><small>Источник</small><span>@${esc(x.source||'неизвестно')}</span></div><div class="meta-cell"><small>Состояние</small><span>${x.kind==='active'?'Участие открыто':'Перепроверяется'}</span></div></div>${!small&&reason?`<p class="dialog-copy">${esc(reason)}</p>`:''}<div class="actions">${u?`<button class="button primary" data-action="open" data-url="${esc(u)}">Открыть</button>`:''}<button class="button ${mine?'success':'secondary'}" data-action="join" data-id="${esc(id)}">${mine?'✓ Участвую':'Я участвую'}</button></div>${!small?`<div class="actions" style="margin-top:8px">${post?`<button class="button ghost" data-action="telegram" data-url="${esc(post)}">Пост</button>`:''}<button class="button ghost" data-action="share" data-id="${esc(id)}">Поделиться</button><button class="button ghost" data-action="wheel-info" data-id="${esc(id)}">Подробнее</button></div>`:''}</article>`}

function greeting(){const h=new Date().getHours(),name=user()?.first_name?`, ${user().first_name}`:'';return h<6?`Доброй ночи${name}`:h<12?`Доброе утро${name}`:h<18?`Добрый день${name}`:`Добрый вечер${name}`}
function renderHome(){const all=wheels(),active=all.filter(x=>x.kind==='active'),next=active.find(x=>x.deadlineObj&&x.deadlineObj>Date.now())||active[0]||all[0],t=totals(1),h=healthStatus(),mine=all.filter(joined).length;$('#page-home').innerHTML=`<div class="page-title-row"><div><h1>${esc(greeting())}</h1><p><span class="status-dot ${h.level}"></span>${esc(h.label)} · ${esc(h.age.text)}</p></div></div>${next?`<article class="hero-card"><div class="hero-top"><span class="eyebrow">Ближайшее колесо</span>${badge(next)}</div><h2>${esc(next.identifier||next.key)}</h2><span class="countdown" ${next.deadlineObj?`data-deadline="${esc(next.deadlineObj.toISOString())}"`:''}>${esc(next.deadlineObj?left(next.deadlineObj):'на перепроверке')}</span><p class="hero-subtitle">Источник: @${esc(next.source||'неизвестно')}</p><div class="hero-actions">${safeUrl(next.url)?`<button class="button primary" data-action="open" data-url="${esc(safeUrl(next.url))}">Открыть</button>`:''}<button class="button ${joined(next)?'success':'secondary'}" data-action="join" data-id="${esc(wheelKey(next))}">${joined(next)?'✓ Участвую':'Я участвую'}</button></div></article>`:`<div class="empty-state"><div class="empty-icon">◉</div><h3>Действующих колёс нет</h3><p>Новое колесо появится здесь автоматически.</p></div>`}<div class="metrics-grid"><div class="metric-card"><div class="metric-icon">◉</div><strong>${active.length}</strong><span>действующих колёс</span></div><div class="metric-card"><div class="metric-icon">✓</div><strong>${mine}</strong><span>моих отметок</span></div><div class="metric-card"><div class="metric-icon">◌</div><strong>${app.data.primary.length}</strong><span>основных источников</span></div><div class="metric-card"><div class="metric-icon">↻</div><strong>${compact(t.checks)}</strong><span>проверок сегодня</span></div></div><section class="section"><div class="section-head"><h2>Ближайшие колёса</h2><button class="text-button" data-route="wheels">Все колёса</button></div><div class="wheel-list-grid">${all.slice(0,3).map(x=>wheelCard(x,true)).join('')||'<div class="empty-state"><p>Список пока пуст.</p></div>'}</div></section><section class="section"><div class="section-head"><h2>Система</h2><button class="text-button" data-route="profile">Подробнее</button></div><article class="list-card"><div class="setting-row"><div class="setting-copy"><strong>${esc(h.label)}</strong><small>Последняя проверка: ${esc(fmt(h.last))}</small></div><span class="status-dot ${h.level}"></span></div><div class="setting-row"><div class="setting-copy"><strong>${h.ok} источников доступны</strong><small>Ошибок: ${h.errors}</small></div><span>${h.errors?'!':'✓'}</span></div></article></section>`}

function renderWheels(){const all=wheels(),filtered=all.filter(x=>app.filter==='active'?x.kind==='active':app.filter==='pending'?x.kind==='pending':app.filter==='mine'?joined(x):true),labels={all:`Все ${all.length}`,active:`Действуют ${all.filter(x=>x.kind==='active').length}`,pending:`Проверяются ${all.filter(x=>x.kind==='pending').length}`,mine:`Мои ${all.filter(joined).length}`};$('#page-wheels').innerHTML=`<div class="page-title-row"><div><h1>Колёса</h1><p>Актуальные находки и личные отметки</p></div></div><div class="chip-row">${Object.entries(labels).map(([k,v])=>`<button class="chip ${app.filter===k?'active':''}" data-filter="${k}">${esc(v)}</button>`).join('')}</div><div class="wheel-list-grid">${filtered.map(x=>wheelCard(x)).join('')||'<div class="empty-state"><div class="empty-icon">◉</div><h3>Список пуст</h3><p>Подходящих колёс сейчас нет.</p></div>'}</div>`;const count=all.filter(x=>x.kind==='active').length,b=$('#wheelNavBadge');b.textContent=count>9?'9+':count;b.classList.toggle('hidden',!count)}

function chart(days){const count=days===30?14:Math.max(1,days),rows=[];for(let i=count-1;i>=0;i--){const d=new Date;d.setDate(d.getDate()-i);const k=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;rows.push({d,n:Number(app.data.stats?.daily?.[k]?.totals?.checks||0)})}const max=Math.max(1,...rows.map(x=>x.n));return`<div class="bar-chart">${rows.map(x=>`<div class="bar-column"><div class="bar-stack"><span class="bar-value">${x.n?compact(x.n):''}</span><i class="bar" style="height:${Math.max(x.n?4:1,Math.round(x.n/max*100))}%"></i></div><span class="bar-label">${x.d.toLocaleDateString('ru-RU',{day:'2-digit',month:days===1?undefined:'2-digit'})}</span></div>`).join('')}</div>`}
function rankRow(x,i){return`<button class="rank-row" data-action="source-info" data-source="${esc(x.source)}" style="width:100%;border:0;background:transparent;color:inherit;text-align:left"><span class="rank-number">${i+1}</span><span class="source-copy"><strong>@${esc(x.source)}</strong><small>Активаций: ${x.acts}</small></span><span class="rank-metric"><strong>${x.wheels}</strong><small>колёс</small></span></button>`}
function renderStats(){const t=totals(app.days),h=healthStatus(),all=wheels();$('#page-stats').innerHTML=`<div class="page-title-row"><div><h1>Статистика</h1><p>Работа монитора и рейтинг источников</p></div></div><div class="stat-periods">${[1,7,30].map(x=>`<button class="period-button ${app.days===x?'active':''}" data-days="${x}">${x===1?'Сегодня':`${x} дней`}</button>`).join('')}</div><div class="metrics-grid"><div class="metric-card"><div class="metric-icon">↻</div><strong>${compact(t.checks)}</strong><span>проверок</span></div><div class="metric-card"><div class="metric-icon">▤</div><strong>${compact(t.messages_scanned)}</strong><span>сообщений</span></div><div class="metric-card"><div class="metric-icon">◉</div><strong>${num(t.wheel_posts)}</strong><span>постов с колёсами</span></div><div class="metric-card"><div class="metric-icon">≠</div><strong>${compact(t.duplicates_suppressed)}</strong><span>повторов скрыто</span></div></div><section class="section"><article class="card chart-card"><div class="chart-title"><h3>Проверки по дням</h3><span class="badge">${app.days===1?'сегодня':`${app.days} дней`}</span></div>${chart(app.days)}</article></section><section class="section"><div class="section-head"><h2>Сводка</h2></div><article class="list-card"><div class="setting-row"><div class="setting-copy"><strong>Действующих колёс: ${all.filter(x=>x.kind==='active').length}</strong><small>На перепроверке: ${all.filter(x=>x.kind==='pending').length}</small></div><button class="text-button" data-route="wheels">Открыть</button></div><div class="setting-row"><div class="setting-copy"><strong>${esc(h.label)}</strong><small>${esc(h.age.text)}</small></div><span class="status-dot ${h.level}"></span></div><div class="setting-row"><div class="setting-copy"><strong>Ошибок: ${num(t.errors)}</strong><small>Подтверждено активных: ${num(t.activation_sent)}</small></div><span>${t.errors?'!':'✓'}</span></div></article></section><section class="section"><div class="section-head"><h2>Рейтинг источников</h2></div><article class="list-card">${ranking().slice(0,10).map(rankRow).join('')||'<div class="empty-state"><p>Рейтинг ещё накапливается.</p></div>'}</article></section>`}
