name: Archived Mini App state API (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Worker and D1 are frozen
        run: |
          echo "The Mini App Worker/D1 deployment is archived and disabled."
          echo "The Telegram bot does not depend on this workflow."
