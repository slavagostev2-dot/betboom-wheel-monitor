name: Archived Mini App deployment (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Mini App is frozen
        run: |
          echo "Mini App deployment is archived and disabled."
          echo "Restore it only after an explicit user request to resume Mini App development."
