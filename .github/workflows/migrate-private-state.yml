name: Archived Mini App state migration (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: D1 migration is frozen
        run: |
          echo "Mini App state migration is archived and disabled."
          echo "Telegram bot users are stored by the bot-only encrypted state workflow."
