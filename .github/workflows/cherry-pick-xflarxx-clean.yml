name: Deprecated xFLARXx preparation placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "This obsolete preparation workflow is disabled and retained only until repository cleanup."
