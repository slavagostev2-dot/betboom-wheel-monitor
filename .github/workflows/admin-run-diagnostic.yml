name: Temporary admin run diagnostic

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/admin-run-diagnostic.yml"

permissions:
  actions: read
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Show failed v27 production log
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh run view 29331725375 \
            --repo "${{ github.repository }}" \
            --log-failed
