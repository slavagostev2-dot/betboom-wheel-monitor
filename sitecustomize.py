from __future__ import annotations

# Intentionally empty.
#
# Production behavior must be installed explicitly by the bot, monitor, report,
# discovery and system-check entry points. This prevents archived Mini App / D1
# code from changing the Telegram bot implicitly at Python startup.
