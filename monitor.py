# Анализ Chat Стаи Ставки — экспорт 12.07.2026

Экспорт использован только для выделения ссылок `betboom.ru/freestream/`, статистики идентификаторов и сопоставления с публичными Telegram-источниками. Имена участников и обычные сообщения в репозиторий не переносились.

## Итоги

- Сообщений в экспорте: **1 000 759**
- Сообщений со ссылками на колёса: **290**
- Уникальных идентификаторов: **89**
- Новых идентификаторов для исторической базы: **28**

## Часто встречавшиеся идентификаторы

| Идентификатор / группа | Количество публикаций |
|---|---:|
| `zonertg*`, `zonertw*`, `zonerwheel` | 61 |
| `solo*`, `solotg` | 23 |
| `dyrachyo` | 21 |
| `papa` | 21 |
| `cct1` | 20 |
| `nix*` | 14 |
| `bolt` | 13 |
| `aunkere` | 12 |
| `staya*` | 12 |
| `krat` | 10 |
| `neret` | 9 |
| `sigma` | 8 |
| `ewc` | 7 |

## Подтверждённые сопоставления с публичными источниками

- `zoner*` → `@mechanogun`
- `dyrachyo` → `@dyrachyo`
- `papa` → `@PAPAdota2`
- `solo*` → `@solo_q_q`
- `nix*` → `@nixtalk`
- `bolt` → `@boltcast`
- `aunkere*` → `@aunkereEZ`, `@aunkeretg`
- `staya*` → `@obshakstaya`
- `adam` → `@AdamStaya`
- `neret` → `@NeretCast`
- `stray*` → `@StrayDungeon228`
- `shoke` → `@shoketg`
- `over` → `@ihuntnoobs`
- `deko` → `@dekocs`, `@dekocsoff`
- `MAGER*` → `@mAger_de_nuke`
- `CTOM*` → `@meowbettt`, `@ct0mislove`
- `acool*` → `@acoolbazarit`
- `chopper*` → `@chopperis`
- `HOOCH*` → `@hoochcs2`
- `LOLLY*` → `@whylollycrry`
- `avice*` → `@aviciixd`
- `blindzone` → `@Blindzonexbet`, `@blindzonexgod`
- `dayneez` → `@DayneezBet`, `@dayneZz`
- `danila.gorilla`, `gorilla` → `@danila_gorilla`, `@gorilla_armor`
- `krat` → `@KRATtv`
- `shadowkek` → `@shadowkekw`, `@burdakekw`
- `relax` → `@relaxcis`, `@RelaxCisBet`

Машиночитаемая версия этих сопоставлений находится в `identifier_sources.json`. Она используется только для подписи уведомлений и анализа. Старые адреса `/freestream/...`, включая исторический `shoke`, по этому файлу не опрашиваются.

## Сборщики и публичные чаты

В быстрый Telegram-мониторинг включены публичные источники, где могут появляться ссылки разных авторов:

- `@homakolesa`
- `@narodCast`
- `@dartwager`
- `@frixa_betboom`
- `@amam0610`
- `@gazazor`

## Правила после объединения файлов

- основной поиск выполняется только через новые публикации публичных Telegram-каналов и чатов;
- одна и та же ссылка из разных источников не отправляется повторно;
- новая ссылка открывается один раз для попытки извлечь таймер;
- повтор блокируется до прокрутки плюс 30 минут, а при неизвестном времени — на 24 часа;
- быстрый и ночной списки разделены: подтверждённые источники проверяются каждые 5 минут, дополнительные кандидаты — ночью;
- неподтверждённые каналы, добавленные только из-за совпадения имени со старым идентификатором, исключены.

Одноразовые и неочевидные идентификаторы (`PFMHP6WT`, `O0XEAX75` и подобные) остаются историческими данными и не привязываются к случайному Telegram-каналу без подтверждения.
